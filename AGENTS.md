# Windows 会话识别与密钥扫描约束

本项目可以提供 OCR 和数据库等可配置的会话识别策略。进程内存扫描仅用于提取微信数据库密钥，不能作为识别当前会话或读取新消息的常规步骤。

首个会话识别插件及后续 CRM 登录门禁、微信多开方案记录在 `docs/PLUGIN_ARCHITECTURE.md`；其中仅明确标注已实现的部分可视为现有功能。

实施插件化时必须采用小步改动：保留现有 OCR 和 `core/engine.py` 行为，按独立、可验证的步骤加入插件接口、策略编排及后续能力，不得借此重写主流程。

- 默认只读当前窗口 UIA 标题，缺失或冲突时回退窗口级 OCR。两种模式均不得调用密钥提取器、`OpenProcess`/`ReadProcessMemory` 等进程内存扫描路径。
- 自动关联窗口与微信账号时，只可查询对应进程已打开的数据库文件句柄；这是只读身份线索，不得读取进程内存、提取密钥，或在证据不唯一时猜测账号。
- 按账号复用已保存的数据库密钥。启动程序、登录或切换账号、打开或切换会话、标题或消息区域变化、消息/WAL 刷新、数据库快照更新，以及识别策略的轮询或回退，均不得自动触发进程内存扫描。
- 只有用户在设置界面明确点击“提取密钥”后，才允许启动一次密钥扫描。此操作应独立于会话识别流程；即使用户把数据库策略排在最前面，也不能绕过显式触发要求。
- 密钥缺失、校验失败、新增数据库分片或扫描失败时，提示用户采取下一步操作，不得在后台自动扫描或重试扫描。用户再次明确点击“提取密钥”才可重试。
- 数据库密钥和扫描结果不得写入日志。密钥文件仅保存在当前账号的本地缓存中，不上传，也不纳入版本控制或发布包。
- 原始微信 DB/WAL 只允许按字节复制到程序 `data/` 下的账号缓存；密钥校验、解密、SQLite 查询和历史读取只操作副本。不得对原库建立 SQLite 连接或修改原库。只有完整校验的副本才可发布给识别器。
- 秒级刷新只轮询当前账号的 DB/WAL 文件状态，复制 WAL 新增字节并在本地验证已提交帧；读写本地副本须跨进程协调。WAL 重置或完整性异常不得猜测缺失消息，需重同步受影响分库或提示用户；绝不可借刷新触发内存扫描。
- 对缺少 `sync.json` 的旧快照，因没有可信 WAL 游标，仅允许在后台按文件状态重复制当前账号的 `session.db/WAL` 和联系人 `contact.db/WAL`，在程序缓存内解密并完整校验后原子替换对应副本；不得据此自动重建消息大库、读取原库 SQLite 或扫描进程内存。首次读到的清未读值只能作为基线。
- `SessionTable.last_clear_unread_timestamp` 只能作为当前窗口切换附近新发生的辅助线索；历史最大值、没有变化的零未读会话、跨账号记录不得单独确认身份。同名联系人还须有该联系人的可见消息证据且其他同名候选不匹配。
- 短消息消歧只能在同名且常规证据不足时触发；须核对至少三条可见消息的顺序、屏幕时间与该账号的消息库，并排除其他同名候选。不能只因某联系人有会话记录或常见短文本就确认。
- 同名候选的手动选择必须由用户显式确认，并标记为用户确认，不得冒充自动识别；只允许从当前账号明文快照的候选中选，绑定当前窗口及选中行，窗口、账号、标题或选中行改变时立即失效。填入前须重新核对，校验失败禁止填入。没有稳定选中行时只能绑定可见证据，证据变化即失效。
- 修改密钥或会话识别逻辑时，验证 OCR 路径不触发扫描、缓存密钥可跨启动复用、各类失败不会自动扫描，以及一次用户操作不会意外启动多个并发扫描。
